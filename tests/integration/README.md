# Integration tests

Empty for now. Integration tests will be added starting Task 2+, once
repositories and services exist to integrate against (database, RBAC
policies, etc.). Test runner configuration (Vitest) already picks up this
directory via `vitest.config.ts`.
